{
    "name": "RNExistingApp",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "node node_modules/react-native/local-cli/cli.js start --config ../../../../packager/config.js --reset-cache",
        "start:unbundle": "export BUNDLE_COMMAND=unbundle && export BUNDLE_CONFIG=packager/config.js && yarn run start",
        "start:bundle": "export BUNDLE_COMMAND=bundle && export BUNDLE_CONFIG='' && yarn run start",
        "test": "jest"
    },
    "dependencies": {
        "react": "16.2.0",
        "react-native": "0.52.2"
    },
    "devDependencies": {
        "babel-jest": "22.1.0",
        "babel-preset-react-native": "4.0.0",
        "jest": "22.1.4",
        "react-test-renderer": "16.2.0"
    },
    "jest": {
        "preset": "react-native"
    }
}